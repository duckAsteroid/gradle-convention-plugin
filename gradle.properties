# This repo targets Java 21 for its own build (see CLAUDE.md), separate from the Java 25 default
# that duckasteroid-java hands out to consumers - overridden via its own documented extension point
# rather than fighting the toolchain block a second time.
duckasteroid.java.version=21
